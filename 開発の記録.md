# ChatGPTでアプリ開発（基本はコピペのみ）

## 環境構築

githubのリポジトリ作成は手動で実施。
Reactプロジェクト作成は質問の回答のみ手動。
ChatGPTがプロジェクト初期化の手順を吐いてくれるので、コマンドをコピペでおk。

```bash
npm install
npm run dev
```

その後ブラウザで動作確認を指示されたので、指定されたURLにアクセス。→アクセスおk！
`http://localhost:5173s`

## 実装開始

- ChatGPTが実装する機能の提案をしてくれる。

```ChatGPT
🚀 次のステップ提案
この状態から、以下を進めていけます：

✅ TodoInput.jsx を追加（タスク入力）

✅ TodoList.jsx を追加（タスク一覧表示）

✅ GptInput.jsx を追加（ChatGPT API連携）

準備ができていたら「続けて」と言ってくれれば、すぐに TodoInput.jsx のコードから再実装始めます！
```

- たまにハルシネーション（嘘）が発生する

```ChatGPT
🧩 実装ポイント
各タスク横に「削除」ボタンが表示されます

押すと、そのタスクだけが tasks 配列から除かれ、即座に再描画

スタイリング付き（赤字でホバー時に色濃く）←ここ、ホバーしてもボタンの枠が青く変化するだけ
```

- 「リロードしてもタスクが削除されなくなりました（嘘）」

リロードしたら登録済みのタスクが削除された点を指摘。\
すると改修したソースを提示してきたので、それを動かすとリロードしても登録済みのタスクが削除されなくなった。🌸💯


## Tailwindcssを入れてなかったね

初期設定コマンドがこける

```bash
npx tailwindcss init -p    

npm error could not determine executable to run
npm error A complete log of this run can be found in: /Users/tchuma/.npm/_logs/2025-06-08T07_19_32_711Z-debug-0.log
```

```ChatGPT
tailwindcssのバージョンが^4.1.8となっていますが、現在の最新の安定版は3.x系です
postcssのバージョン^8.5.4も存在しないバージョンです
```

なるほど、バージョンの指定を間違えたらしい。
企画からここまで2時間半

## 「次の機能はChatGPTでタスクを追加」

自分「APIキー未取得なので一旦スキップで」

## どんどん機能追加

ソートやチェックボックスやタグなど様々な機能を追加
ダークモード切り替えのUIが好みじゃなかったので修正依頼

「ークモード切り替えをトグルボタンに変更、アイコンは太陽と月で」

```bash
npm install lucide-react
```

でパッケージをインストール

## 🚀 公開リンク

[ChatGPT TODO App](https://chuma-asys.github.io/chatgpt_todo/)


## ✅ 実装済み機能まとめ

- タスクの追加・削除・完了
- 期限・タグ付き登録
- ローカルストレージ永続化
- 並び順の切替（新しい順 / 古い順）
- 完了タスクの一括削除
- ダークモード切替（スライドUI付）
- 検索＆未完了フィルター
- 完了タスク統計表示
- タスクのピン留め（固定表示）

## 感想

- 3時間でそこそこな機能を持ったアプリができた
- 開発スピードが早すぎてコミットのタイミングを逃す、クソデカコミットになる→レビュアーがキツい
- 今回、最低限必須だったスキルはReactの環境構築、Git知識、エラー対応くらい
- 感動した、やってよかった
